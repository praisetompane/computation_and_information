/* 
 * File:   main.cpp
 * Author: t2p
 *
 * Created on 13 July 2015, 11:12 PM
 */

#include <cstdlib>
#include <iostream>
using namespace std;

/*
 * 
 */
int main(int argc, char** argv) {

    /*int i = 3, j, k;
   if (i > 0) j = 2 + i * i;

   if (i <= 0) j = 2 * i - 1;

   if (j >= 0) k = j % i + 2;

   if (j < 0) k = i % j + 2;

   if (k < 0) k = k % i % j;

   if (k >= 0) {
       k = j % i % k;
      cout << k;
   }
   std::cout << k << "" << j << i << " " << j % i % k;*/ ̰
    int i = 3, j = ++i, k = ++i;
    std::cin >> i;
    std::cout << k + i << j - i << i * i;

    //int a = X10;
    
    cout << 0x1A;

    return 0;
}

