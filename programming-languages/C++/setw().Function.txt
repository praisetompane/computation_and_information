#include <iostream>
#include <iomanip>
using namespace std;

int main()
{
    //Allows to create spacing
    cout << setw(4) << 18 <<"\n";
    //Ceate decimal positions{seems to include the . point]
    cout << setprecision(3) << 9.454894084 <<endl ;
    system ("Pause");
}
