package week1

object session {
                                                  //> res0: Int(2) = 2
	def sqrtIter(guess: Double, x: Double) : Double = 
	if(isGoodEnough(guess,x)) guess
	else sqrtIter(improve(guesss,x),x)

}