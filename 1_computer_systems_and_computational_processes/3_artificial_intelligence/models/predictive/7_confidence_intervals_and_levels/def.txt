a range of values within which 
    we expect a random value to exist
    and the confidence level(as a percentage) about its existence in the range

empiral rule

normal distribution easy to generate? Why?
