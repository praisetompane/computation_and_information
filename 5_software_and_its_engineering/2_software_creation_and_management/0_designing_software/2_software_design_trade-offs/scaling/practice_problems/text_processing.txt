Context:
    Given a list of millions of documents.

Objective:
    Implement a word search algorithm
        that will return all documents
        with that word

Constraints:
    Must be full word match
        i.e. given word = book,
            bookkeeper, books etc DO NOT match.

Definitions:
    a word = sequence of english characters