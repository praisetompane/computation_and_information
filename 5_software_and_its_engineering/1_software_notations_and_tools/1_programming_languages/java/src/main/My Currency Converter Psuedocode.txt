Get num
determine decimal

Get length

	case (length)
		perform conversion here
			:Look for num in word array.
			 Concat result from word array
			 
			 if(decimal)
				add "and"
					Look for decimal digits
					Look for decimal digits