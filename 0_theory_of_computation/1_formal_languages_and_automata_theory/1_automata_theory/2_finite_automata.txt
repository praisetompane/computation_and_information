def finite automata:
    formal:
        a finite automata M is a 5-tuple(Q, Σ, δ, q₀, F,) (Sipser, 2020):

        Q  = finite set of all states
        Σ  = finite set of alphabet symbols
        q₀ = start state/initial state
        F  = finite set of final states = set of accept states
        δ  = transition function
            δ: Q × Σ => Q : this is function definition
                    δ       : function name
                    Q × Σ   : parameter types
                    Q       : function output/return type
            example:
                δ(q, a) = r
                
                diagrammatic version:
                         a
                    q -------> r
            in words:

    plain english:

    intuition:

    examples:

                    
    diagrammatic representation of components
        states
            initial - required
                arrow coming in from nowhere pointing to state
            final - required
                two circles
            rest of states = circle
        edges
            pointed arrow
                label = input = defined in Σ
                         a
                    q -------> r
    
two broad categories (Neso Academy).
    2.1. FA with output 
    2.2. FA without output

    2.1. 
        2.1.1. Moore machine
        2.1.2. Mealy machine

    2.2
        2.2.1. Deterministic Finite Automata - DFA
        2.2.2. NonDeterministic Finite Automata - NFA

References:
    Neso Academy. 2016. Finite State Machine (Finite Automata). https://www.youtube.com/watch?v=Qa6csfkK7_I&list=PLBlnK6fEyqRgp46KUv4ZY69yXmpwKOIev&index=3
    Sipser, M. 2020. Introduction to the Theory of Computation. https://ocw.mit.edu/courses/18-404j-theory-of-computation-fall-2020/resources/introduction-finite-automata-regular-expressions/.
